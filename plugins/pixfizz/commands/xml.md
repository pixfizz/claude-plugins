---
description: Create, review or debug an XML template definition
---

Load the `xml-templates` skill from this plugin.

Establish the product archetype first and start from the closest one rather than an empty file. For a dated product, also load the calendar reference. Remember that `width` and `height` are the pre-trimmed production size, bleed is a guide only, and margin is measured from the bleed line inward.

$ARGUMENTS
