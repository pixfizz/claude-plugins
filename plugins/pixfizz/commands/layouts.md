---
description: Create or fill photo book layouts in a Pixfizz design theme
---

Load the `photobook-layouts` skill from this plugin and run its workflow.

Ask for the exported `__print_theme.yml` if one has not been provided, and establish the page geometry before generating anything. Follow the skill's rule on blank layouts created in admin so every record keeps a real platform id, and run the verifier before handing the file back for re-import.

$ARGUMENTS
