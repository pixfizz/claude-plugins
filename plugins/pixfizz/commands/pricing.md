---
description: Write, review or debug a Pixfizz pricing formula
---

Load the `pricing-formulas` skill from this plugin.

Settle the four questions first — photo print or not, `quantity` vs `units`, whether page count drives the price, and where the formula lives. Do not write a formula until they are answered. Never invent a rate: if the client has not supplied the numbers, ship the ladder with zeros and say so.

Run any formula you produce in real Ruby, including below-range, above-range and boundary cases, before presenting it.

$ARGUMENTS
